'use strict';

describe('myApp.flexBox2 module', function() {

  beforeEach(module('myApp.flexBox2'));

  describe('flexBox2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});